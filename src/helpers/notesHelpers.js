/**
 *
 * @param {*} note {AzL35x7QigITy8Yo3IVQ: {title: 'Lorem title', body: 'Lorem itsum body ',...}}
 * @return {} {title: 'Lorem title', body: 'Lorem itsum body ',...}
 */

/* export const getNoteObject = (note) => {
  for (const id in note) {
    return note[id];
  }
};
export const hasEntries = (notes) => {
  for (const key in notes) {
    return true;
  }
  return false;
};
 */
